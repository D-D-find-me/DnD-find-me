# Migration `20201231171939-dn-d-find-me`

This migration has been generated by Brian Demro at 12/31/2020, 11:19:39 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201231171939-dn-d-find-me
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,53 @@
+generator client {
+  provider = "prisma-client-js"
+}
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+model adventurers {
+  id         Int         @id @default(autoincrement())
+  username   String
+  password   String
+  char_class String
+  zipcode    String
+  dm         Boolean
+  online     Boolean
+  pfp        String
+  phone_num  String?
+  comments   comments[]
+  locations  locations[]
+  posts      posts[]
+}
+
+model comments {
+  id           Int          @id @default(autoincrement())
+  body         String
+  commentor_id Int?
+  created_at   DateTime     @default(now())
+  post_id      Int?
+  adventurers  adventurers? @relation(fields: [commentor_id], references: [id])
+  posts        posts?       @relation(fields: [post_id], references: [id])
+}
+
+model locations {
+  id          Int          @id @default(autoincrement())
+  createdat   DateTime     @default(now())
+  latitude    Float?
+  longitude   Float?
+  adv_id      Int?
+  adventurers adventurers? @relation(fields: [adv_id], references: [id])
+}
+
+model posts {
+  id          Int          @id @default(autoincrement())
+  title       String
+  content     String
+  zipcode     Int
+  created_at  DateTime     @default(now())
+  adv_id      Int?
+  adventurers adventurers? @relation(fields: [adv_id], references: [id])
+  comments    comments[]
+}
```


